{
  "build": {
    "env": {
      "PRISMA_GENERATE_SKIP_AUTOINSTALL": "false",
      "DATABASE_URL": "${DATABASE_URL}",
      "DIRECT_URL": "${DIRECT_URL}"
    }
  },
  "functions": {
    "src/**/*.ts": {
      "runtime": "nodejs20.x"
    }
  }
}